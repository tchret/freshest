<div class="navigation-bar">
  <div class="container">
    <%= link_to root_path, class: 'brand-link' do %>
      <div class="brand">
        <div class="pull-left">
          <span class='miniature'>
            <span>F</span>
          </span>
        </div>
        <div class="pull-left">
          <div class="logo">
            <span>Freshest</span>
          </div>
          <div class='slogan'>
            Latest News // Best Sources
          </div>
        </div>
      </div>
    <% end %>
    <div class="bar-right pull-right">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li>
            <%= react_component("ModalSource", {
              currentUser: current_user.slice(:twitter_id)
              }) %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle margin-top-4" data-toggle="dropdown" role="button" aria-expanded="false">
              <%= image_tag current_user.picture, { class: "avatar-user" } %>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Sign out', destroy_user_session_path, method: 'delete' %></li>
            </ul>
          </li>
        </ul>
      <% else %>
      <%= react_component("ModalConnect", {
        href: user_omniauth_authorize_path(:twitter),
        last_sources_images: @sources[0..4].map {|source| source.avatar_url},
        opened: false
        }) %>
      <% end %>
    </div>
  </div>
</div>
<div class='sub-navigation-bar'>
  <div class="container">
    <%= link_to 'Read Stuff', root_path, class: 'reading-list-link' %>
    <%= link_to 'Dashboard', sources_path, class: 'dashboard-link' %>
  </div>
</div>